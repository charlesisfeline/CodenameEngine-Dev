package funkin.options.type;

class Separator extends TextOption {
	public function new() {
		super("", "", null);
		optionHeight = 30;
		// perhaps this might be the trashiest way to make a separator thing
	}
}